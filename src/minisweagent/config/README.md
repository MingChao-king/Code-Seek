# Configs

* `mini.yaml` - Default configuration for the continuous `AssistantAgent` CLI and Python entry points.

## Benchmarks

* `benchmarks/swebench.yaml` - Config for the `run/benchmarks/swebench.py` entry point.
